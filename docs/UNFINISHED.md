# 未完成与未验收清单

已有代码和历史测试报告不等于用户验收；本次迁移不重新宣称运行恢复。

## P0：真实日常闭环

- [ ] 当前 macOS arm64 实际入口 SSH 登录、连续两笔传输，Running/Pending/History 同时正确。
- [ ] 后端事件到真实 GUI、成功/失败/取消与历史重启恢复；不能只用 mock。
- [ ] 远端 rsync/SCP/加密流真实字节进度或可核验步骤/活性，现有动画不是数据推进证据。
- [ ] Shell 登录环境、提示符残留、输入编辑、cwd 双向同步和每条命令刷新，连续使用验证。
- [ ] 拖到目录/空白、无选中 Backspace、不选文本、覆盖合并、小窗口第三列，成品回归。
- [ ] 远端到本机保护目录权限处理、提权确认、失败保留源，不能用反复策略重试掩盖 permission denied。
- [ ] 用户 Markdown 多跳/vault key/Shell/密码遮罩/物理行号真实保存重连与重启验证。

## P1：复杂策略与安全

- [ ] 四种方向/权限和失败回退矩阵，特别是真实非 root → sudo。
- [ ] 成功跳板主机对缓存、失效重探测，避免遍历不适合连接的主机。
- [ ] SOCKS/SSH/Hans 层没有完整重放 ncat：原计划偏差待安全实现或用户明确接受，不以凭据进入 argv 换取完成。
- [ ] 官方 Hans 当前成品的数据链、角色反转、确认、取消和清理。
- [ ] 控制机零落盘、目标原子提交、源变更/哈希不一致不删除、取消清理的集成验收。
- [ ] 指纹变化阻断、认证优先级、SFTP 回退、脱敏、vault 路径和权限的真实入口验证。
- [ ] GitHub workflow 持续验证；本次迁移不声称 CI 已通过。

## 明确延期或不在范围

- 六平台产物和 Windows/Linux 原生冒烟，等 macOS arm64 验收。
- 旧 Fyne 离线 Linux libGL/X11 修复，用户明确暂不处理。
- 无桌面服务器 Web UI 只是建议，未授权。
- 发布前说明 Linux GUI 系统依赖，不能宣传任意服务器零依赖运行。
- Developer ID 签名/公证与其他平台签名尚未落实，历史 macOS 产物仅 ad-hoc。

按完整用户流程批次交付，不用局部 PASS 覆盖主入口失败。
